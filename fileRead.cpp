// to find 5th record
#include<stdio.h>
#include <cstring>
int main(){
	FILE *fp;
	fp=fopen("f1.txt","rb");
fseek(fp,(n-1)*sizeof(stud),0);
fread(&s1,sizeof(s1),fp);
printf("%d %s %f",s1.id.s1.name,s1,marks)
		
	
	printf("file is created");
}
